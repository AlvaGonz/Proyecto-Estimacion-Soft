# Examples for planning-base

## Standard Workflow
1. User requests: "Begin planning the new feature using the persistent file-based workflow."
2. Agent reads SKILL.md.
3. Agent copies templates from 'templates/' to the project root.
4. Agent fills out the artifacts following the hard rules.