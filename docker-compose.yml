<<<<<<< HEAD
version: '3.8'

services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: proyecto-estimacion-soft-frontend-1
    ports:
      - "${FRONTEND_PORT:-3000}:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - app-network

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: proyecto-estimacion-soft-backend-1
    ports:
      - "${BACKEND_PORT:-4000}:4000"
    env_file:
      - .env.docker
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:4000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s

  mongo:
    image: mongo:7
    container_name: proyecto-estimacion-soft-mongo-1
    ports:
      - "${MONGO_PORT:-27017}:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASS:-secret123}
      MONGO_INITDB_DATABASE: estimacion-dev
    volumes:
      - mongo-data:/data/db
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "mongosh", "--username", "${MONGO_ROOT_USER:-admin}", "--password", "${MONGO_ROOT_PASS:-secret123}", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # Servicio seed — se ejecuta UNA vez y termina
  seeder:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: proyecto-estimacion-soft-seeder-1
    command: ["node", "dist/seed.js"]
    env_file:
      - .env.docker
    depends_on:
      mongo:
        condition: service_healthy
    networks:
      - app-network
    restart: "no"   # Solo corre una vez

volumes:
  mongo-data:

networks:
  app-network:
    driver: bridge
=======
version: "3.8"

services:
  mongodb:
    image: mongo:8.0
    container_name: estimacion-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
      - ./init-db.js:/docker-entrypoint-initdb.d/init-db.js
    environment:
      - MONGO_INITDB_DATABASE=Proyecto-Estimacion-Soft

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: estimacion-api
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - MONGODB_URI=mongodb://mongodb:27017/Proyecto-Estimacion-Soft
      - JWT_ACCESS_SECRET=supersecretaccess12345678901234567890
      - JWT_REFRESH_SECRET=supersecretrefresh12345678901234567890
      - JWT_ACCESS_EXPIRY=15m
      - JWT_REFRESH_EXPIRY=7d
      - ALLOWED_ORIGINS=http://localhost:3000
      - RATE_LIMIT_WINDOW_MS=900000
      - RATE_LIMIT_MAX_REQUESTS=100
    depends_on:
      - mongodb
    volumes:
      - ./server:/app
      - /app/node_modules
    entrypoint: ["/app/entrypoint.sh"]
    command: ["npm", "run", "dev"]

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: estimacion-frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:4000/api
    depends_on:
      - api
    volumes:
      - .:/app
      - /app/node_modules

  migrations:
    build:
      context: ./server
      dockerfile: Dockerfile.migrations
    container_name: estimacion-migrations
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/Proyecto-Estimacion-Soft
    depends_on:
      - mongodb
    volumes:
      - ./server:/app
      - ./run-seeds.sh:/app/run-seeds.sh
    # This container exits after running seeds

volumes:
  mongodb_data:
>>>>>>> Dockers-Buid
